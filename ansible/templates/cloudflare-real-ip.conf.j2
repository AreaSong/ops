# Managed by AreaSong/ops. Trust client IP headers only from Cloudflare edges.
{% for cidr in cloudflare_ipv4 + cloudflare_ipv6 %}
set_real_ip_from {{ cidr }};
{% endfor %}
real_ip_header CF-Connecting-IP;
real_ip_recursive on;
