# 服务器台账（结构化）

# 维护规则：
# 1. 新增/释放机器时更新本文件并 git 提交
# 2. 同步更新 servers.md 人类可读摘要
# 3. commit message 格式：[inventory] 描述变更

servers:
  - hostname: LosAngeles
    cloud: crystalclear-solutions-llc
    region: us-ca-los-angeles
    public_ip: "23.185.200.12"
    private_ip: ""
    ansible_user: as
    ansible_ssh_private_key_file: "~/.ssh/id_ed25519_losangeles"
    owner: as
    os: ubuntu-24.04
    roles:
      - web
      - docker-app
      - proxy
    services:
      - nginx
      - x-ui
      - xray
      - resume-jadeai
      - areaforge-web
      - areaforge-postgres
      - sub2api
      - sub2api-postgres
      - sub2api-redis
      - account-vault-web
      - account-vault-postgres
    data_disk: false
    provider_evidence:
      public_ip_rdap_org: "CrystalClear Solutions LLC"
      public_ip_rdap_netname: "CRYSTAL"
      public_ip_rdap_cidr: "23.185.200.0/24"
      public_ip_asn_org: "AS8796 FASTNET DATA INC"
      geo: "Los Angeles, California, US"
      virtualization: "KVM/QEMU"
      cloud_init: "nocloud"
    control_plane:
      control_panel_url: "https://server.zgocloud.cc/"
      instance_name: LosAngeles
      confirmed_at: "2026-07-05"
      confirmed_by: as
      account_mfa_enabled: true
      account_recovery_contacts_available: true
      main_account_shared: false
      api_keys: none
      security_group_or_cloud_firewall: not_supported_by_provider
      snapshots: none
      audit_or_security_notifications: none
      billing_expiry_alerts: deferred_by_user
    phase9_acceptance:
      validated_at: "2026-09-01"
      ops_revision: "9fb1da8fd5ed63a686a7ab7ac9cdf71fd09dba46"
      ops_version: phase9
      ops_release_version: "1.0.3"
      ops_web_image_digest: "sha256:ae3f70e14e2651435ddc4ef740459497059d20a264b013458bde4b6e836d87f5"
      ops_runner_bundle_sha256: "48d6a94e201089609443175021fb83594d4cffa24c119465da6a287a9ebe4b3a"
      running_containers: 19
      prometheus_targets: "23/23 up"
      auditd_rule_keys: 7
      github_issue_token_expires_at: "2027-08-12"
      fresh_backup_manifest: "manifests/backup-set-20260819-074357.json"
      incident_response_tabletop: completed
      cloudflare_access_identity: "song80184@gmail.com"
      legacy_recoverable_tasks: 4
    notes: >-
      主机名不符合规范；ens3 直接使用公网 23.185.200.12/24，未发现主机级 RFC1918 私网 IP；
      Docker bridge 使用 172.17/18/19/20/21 网段；服务已主要规范到 /opt/services；
      旧 /root/JadeAI 与 /root/sorryiosSearch 已归档并删除；云厂商控制台
      https://server.zgocloud.cc/ 实例名 LosAngeles，MFA enabled，邮箱/手机号可用，
      主账号未共用，无 API Key；厂商无安全组/云防火墙/网络规则、快照、云审计/安全通知，
      账单/到期治理暂缓；UFW active: allow 22/80/443 + 9100 仅 172.16.0.0/12，
      default deny incoming；SSH 已禁用 root/password，仅 as key 登录；Fail2ban sshd jail
      enabled: maxretry 5, findtime 10m, bantime 1h；本机备份 enabled:
      /opt/ops/scripts/backup, /var/backups/ops, daily cron, 7d retention；R2 offsite backup
      enabled；监控 enabled: Prometheus/Grafana/Alertmanager/Loki/Promtail/Node Exporter/Blackbox，
      local ports only；Grafana 11.6.16 按 digest 锁定，12 块看板已验收；Loki 留存
      720h/30 天；critical 告警到 GitHub Issue 第二通道 enabled，fine-grained PAT 仅 Issues RW，
      到期日 2027-08-12，每月自动演练；镜像 CVE 周扫描 enabled；GitHub 只读 deploy key
      /root/.ssh/id_ed25519_ops_deploy 仅 fetch；x-ui/xray 流量审计与 TCP 长肥管道调优已启用；
      AreaSong Ops phase9@8961caf 于 2026-08-19 完成服务器侧生产验收；2026-09-01 C2 单人审批发布
      已更新至 release 1.0.3、revision 9fb1da8，Web digest 与 Runner bundle 签名复验通过；
      仓库 unit 已替换旧 Runner 启动路径，Runner socket/health、Web health、build metrics 与
      runtime preflight 均通过。未认证 Web API 负向请求返回 401；2026-08-27 通过 Cloudflare
      Access 身份 song80184@gmail.com 完成服务、状态、对象、Fleet 与 Runner 更新视图的
      authenticated UI-backed API smoke；2026-09-01 再次完成登录后只读 UI smoke，生命周期
      两个受控服务均健康、服务操作与审计页面正常、实时连接正常且前端无 console error，
      未启用能力和平台级权限仍按策略保持关闭。
      AreaForge stop 生命周期 preflight 于 2026-08-27 因 service:areaforge trafficPolicy=null
      按门禁拒绝，未改变生产状态；2026-08-27 已通过 la-share 完成 AreaForge 受控 Nginx
      站点与 snippet 安装、权限校验、nginx -t、reload 和 HTTPS 200 健康检查，traffic 状态
      保持 running，areaforge-web/postgres healthy。sites-enabled 中两个旧备份符号链接仍会造成
      重复 server_name 警告，按本轮批准保留。2026-08-28 经即时明确批准，已先将
      /opt/ops 的 3 个未提交文件备份到 /var/backups/ops/ops-sync-20260827-161153，
      再将仓库 HEAD 与 origin/main 对齐到 57db5852；Sub2API 独立镜像 digest 修改和
      preflight.sh.bak.20260826-1247 均保留。运行制品仍为 c74fe0c2，该 revision 是
      源码 HEAD 的祖先，runtime preflight 通过；2026-08-28 已经明确批准通过 la-share
      写入 service:areaforge TrafficPolicy（摘要 sha256:a9dff122…ba45881），写入后
      JSON、受控 Nginx 文件、nginx -t 与 runtime preflight 均通过；未执行控制面重建、
      Runner 重启、stop/start、reload 或流量切换。

# Ansible 分组（自动生成 inventory 时使用）
groups:
  prod:
    - LosAngeles
  monitor:
    - LosAngeles
  web:
    - LosAngeles
  docker_app:
    - LosAngeles
  proxy:
    - LosAngeles
